import { GuestSigninDirective } from './guest-signin.directive';

describe('GuestSigninDirective', () => {
  it('should create an instance', () => {
    const directive = new GuestSigninDirective();
    expect(directive).toBeTruthy();
  });
});
